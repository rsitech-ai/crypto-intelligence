# Threat Model

Repository-owned implementation artifact for `docs/security/threat-model.md`. It preserves local-only, point-in-time, bounded-resource, calibrated-probability, and no-execution requirements.
