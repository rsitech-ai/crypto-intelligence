schema_version: 1
category: model
status: metadata-only
authoritative_inventory: licenses/artifact-provenance.toml
scope: models
policy: schemas, trust metadata, and a test manifest are present; no model weights are committed
