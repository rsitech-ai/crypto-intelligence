version: v2
modules:
  - path: .
    excludes:
      - admin
      - replay
      - risk
      - settings
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE
