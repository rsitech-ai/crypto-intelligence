version: v2
clean: true
inputs:
  - directory: proto
plugins:
  - local:
      - cargo
      - run
      - --locked
      - --quiet
      - -p
      - xtask
      - --
      - protoc-gen-local-api
    out: crates/local-api/src/generated
    strategy: all
